"""afar — the installation kernel.

Three players (SILT, RUST, KEEP) make music at each other, continuously.
This package is the second instance of the `ensemble` framework.

Naming: `ensemble` is the framework; AFAR (this repo) is the installation
(afar.band); `afar_music` is a separate product (afar.music).
"""

__version__ = "0.0.1"
